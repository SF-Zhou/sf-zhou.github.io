language: node_js

cache: npm

script:
  - node -v
  - npm -v
  - npm install
  - npm run build

deploy:
  provider: pages
  local-dir: public
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: false
  target-branch: master
